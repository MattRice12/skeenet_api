<div class="frames">
  <% frames.each do |frame| %>
    <div class="frame">
      <div><%= frame.number %></div>
      <div><%= frame_scores(frame, player, game, team) %></div>
    </div>
  <% end %>
  <span><%= player_score(player, game, team) %></span>
</div>
